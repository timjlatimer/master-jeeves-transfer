# Jeeves Enforcement Engine — Dependencies
# Python 3.9+

# Web Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Database
# SQLite is built-in to Python — no extra dependency needed

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Utilities (all stdlib, listed for documentation)
# hashlib — built-in
# json — built-in
# sqlite3 — built-in
# dataclasses — built-in
# uuid — built-in
# re — built-in
# os — built-in
